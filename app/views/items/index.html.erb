<table border=1 style="width:100%">
<% @item.each do |x| %>
	<tr>
	<td><%= x.id%></td>
	<td><%= x.name %></td>
	<td><%= x.featured %></td>
	<td><%= link_to x.category_id, categories_path(x.category_id)%></td>
	</tr>
<% end %>
</table>
<p><%= link_to "Main", root_url %></p>